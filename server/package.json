{
  "name": "snake_server",
  "version": "1.0.0",
  "description": "handles all calculations related to the game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "ts-dotenv": "^0.9.0"
  }
}
